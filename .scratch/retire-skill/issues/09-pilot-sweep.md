# 09: Pilot sweep (PR B)

**What to build:** The first real sweep on the live config: the ratification table is presented, Kyle rules by row, the apply pass runs with one confirmation per settings file, the ledger gains its first lines, the re-inventory proves every retired item absent, the PR body carries the measured before/after table and Kyle's `/context` paste, and a review scope is proposed before any merge. Needs Kyle at the table, hence ready-for-human. The plan's twelfth task is the starting point.

**Blocked by:** 07, 08, and PR A merged

**Status:** ready-for-human

- [ ] PR A is merged and `main` is checked out fresh before the sweep starts
- [ ] Pre-check: a skill directory whose skill file is renamed to `.disabled` is absent from a fresh session's skill listing
- [ ] Pre-check: after the loose copies move to the backup folder, bare `/tdd` still resolves to the plugin, or the namespaced spelling is documented in the playbook
- [ ] The ratification table is presented titled, grouped, and collapsed per the spec; the session stops and waits; unanswered rows are not applied
- [ ] Every ratified retirement runs its surface checklist in order; a step that cannot complete stops that item and is reported
- [ ] Settings edits happen only after a per-file confirmation showing the exact edit list, with the file backed up first
- [ ] The ledger has one line per retirement with surface-qualified ids and restore pointers, and kept-with-clause rows in the kept table
- [ ] The re-inventory asserts every retired item absent from every surface it was on; the PR body carries the script's before/after table
- [ ] A review scope is proposed with Preflight evidence and the session stops for Kyle's call

## Comments


# 10: Backlog and idea-doc bookkeeping

**What to build:** The backlog carries the three v2 lanes as stubs in its existing format, and the two ancestor idea docs record their status. The plan's thirteenth task is the starting point.

**Blocked by:** 08

**Status:** done

- [x] Backlog stubs exist for the fleet prune lane, the per-project CLAUDE.md lane, and a periodic `--report` cadence, in the backlog's existing entry format with sizes
- [x] The coliseum idea doc carries a status line saying it was built as the retire skill; the minimal-initial-prompt idea doc says it was absorbed as the CLAUDE.md lane
- [x] No other file changes

## Comments

- 2026-09-18 — merged as `94f39f9`. Three v2 stubs at the top of `## Open` (newest-first, the live convention), matching the house `### [Type] Title` + Why / Acceptance / Size / Added format. Both idea docs carry a status line following the `architecture-viewer.md` precedent.
- Follow-up closed by the orchestrating session in `0548e34`: the plan's Task 13 also wanted the two *backlog items* marked Built / Absorbed, which this ticket's checkboxes did not cover. The Coliseum item's acceptance was satisfied by the skill that just landed, so leaving it open would have been exactly the stale-doc problem the arc exists to prevent.


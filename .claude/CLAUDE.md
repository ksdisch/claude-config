## Agent skills

### Issue tracker

Local markdown under `.scratch/<feature-slug>/`: the spec plus one file per ticket, with a generated `tickets.md` index. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default roles, label strings equal to their names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
